export interface CompeteType {
	points: number[]; //点的集合
	width: number; //宽度
	fill: string; //填充颜色
	type: string; //类型
}

export interface GroupType {
	points: number[]; //点的集合
	width: number; //宽度
	fill: string; //填充颜色
	type: string; //类型
}

export interface RectType {
	x: number;
	y: number;
	width: number;
	height: number;
	fill: string;
}
